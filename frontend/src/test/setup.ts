// Global test setup — runs before every test file.
// Imports jest-dom so matchers like toBeInTheDocument() are available
// in all test files without needing an explicit import each time.
import '@testing-library/jest-dom';
